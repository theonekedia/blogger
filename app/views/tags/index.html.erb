<h1>All Tags</h1>

<ul>
	<% @tags.each do |tag| %>
	<li>
		<li><%= link_to tag, tags_path() %>
		<%= link_to "delete", tags_path(@tag), method: :delete %>
	</li>
	<% end %>
</ul> 
<%= link_to "<< Back to Articles", articles_path %>
</ul>
